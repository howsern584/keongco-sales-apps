# Python packages this project needs.
# Install them with:  pip install -r requirements.txt

fastapi==0.136.3          # the web framework (the "brain" / API)
uvicorn[standard]==0.49.0 # the server that runs FastAPI
sqlalchemy==2.0.50        # talks to the database (SQLite now, PostgreSQL later)
pydantic==2.13.4          # checks/validates data shapes
python-dotenv==1.2.2      # reads settings from the .env file
python-multipart==0.0.32  # lets the API accept file uploads (photos)
